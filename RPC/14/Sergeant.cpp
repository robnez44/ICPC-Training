#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<string> vs;

int main(){

    string s;
    vs words(5);

    cin >> s;
    cin >> words[0] >> words[1] >> words[2] >> words[3] >> words[4];

    size_t pos = 


    return 0;
}